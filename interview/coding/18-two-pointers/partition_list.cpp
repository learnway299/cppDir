/**
 * @file partition_list.cpp
 * @brief 分隔链表 - 面试题
 */

struct ListNode {
    int val;
    ListNode* next;
    ListNode(int x) : val(x), next(nullptr) {}
};

// 题目: 分隔链表 (LeetCode 86)
// 小于 x 的节点在前，大于等于 x 的在后，保持相对顺序
ListNode* partition(ListNode* head, int x) {
    // TODO
    return nullptr;
}

int main() { return 0; }
