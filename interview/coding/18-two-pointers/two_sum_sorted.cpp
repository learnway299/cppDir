/**
 * @file two_sum_sorted.cpp
 * @brief 两数之和（有序数组）- 面试者实现文件
 */

#include "two_sum_sorted.h"

namespace TwoSumSortedImpl {

std::vector<int> twoSum(std::vector<int>& numbers, int target) {
    // TODO: 使用双指针在有序数组中查找两数之和
    // 提示:
    // 1. 初始化 left = 0, right = size - 1
    // 2. 计算 sum = numbers[left] + numbers[right]
    // 3. 如果 sum == target, 返回 {left+1, right+1}
    // 4. 如果 sum < target, left++
    // 5. 如果 sum > target, right--
    return {};
}

} // namespace TwoSumSortedImpl
