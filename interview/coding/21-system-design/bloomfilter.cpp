/**
 * @file bloomfilter.cpp
 * @brief 布隆过滤器 - 面试题
 */
#include <string>

// 题目: 实现布隆过滤器
// 支持:
// - add(key): 添加元素
// - mightContain(key): 判断元素可能存在
class BloomFilter {
public:
    BloomFilter(size_t size, int numHashFunctions) {
        // TODO
    }

    void add(const std::string& key) {
        // TODO
    }

    bool mightContain(const std::string& key) const {
        // TODO
        return false;
    }
};

int main() { return 0; }
